@if (teamDTO is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <TeamForm @ref="teamForm" TeamDTO="teamDTO" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}
