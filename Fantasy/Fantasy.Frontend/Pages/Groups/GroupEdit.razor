@if (groupDTO is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <GroupForm @ref="tournamentForm" GroupDTO="groupDTO" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}
