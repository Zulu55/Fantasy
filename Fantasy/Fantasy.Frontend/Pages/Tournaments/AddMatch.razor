@if (matchDTO is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <MatchForm @ref="addMatchForm" MatchDTO="matchDTO" OnValidSubmit="AddAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}
